# Provenance ledger

One row per salvaged unit. Sources were **copied**, never git-merged; source
repos are read-only. "Uncommitted working tree 2026-08-02" marks files taken
from k25's dirty working tree (branch `local`) that were never committed.

Source commits at time of copy (2026-08-02):

- `k25-nextgen-rewrite` — `fd6f3f467a42a43b379f050df3551b1d9d38c153`
- `k25` — `65b3d88ffc81bd32d0444eb356715cdb751c821c` (branch `local`; archive/ paths clean at this commit)

| Source repo | Commit | Source path | Destination | Intentional changes | Tests carried |
|---|---|---|---|---|---|
| k25-nextgen-rewrite | `fd6f3f4` | `local-server/src/local_server/*.py` (`__init__.py`, `main.py`, `routes.py`, `processing.py`, `models.py`) | `server/src/shizzle_server/` | Package renamed `local_server` → `shizzle_server` (modules use relative imports, so directory rename sufficed). Ruff-baseline fixes: `lifespan(app)` → `lifespan(_app)` (main.py), `JobStatusEnum(str, Enum)` → `StrEnum` (models.py), unused `stem_wavs` param → `_stem_wavs` in `write_manifest` (processing.py), `raise HTTPException(413, …) from None` (routes.py). No behavior changes; contract renames (`default_gain` → `default_gain_db`) deferred to Phase 1.3 | none existed upstream |
| k25-nextgen-rewrite | `fd6f3f4` | `local-server/Dockerfile` | `worker/Dockerfile.reference` | Copied verbatim; becomes the worker's base recipe later | — |
| k25-nextgen-rewrite | `fd6f3f4` | `ui/` (wholesale, 31 files) | `ui/` | Excluded per spec "not carried": `node_modules/`, `dist/`, `README.md`, `IMPLEMENTATION_STATUS.md`, `.env.production`, `.env.example`, `vite.config.local.ts`. Lint fixes to pass ESLint (react-hooks v6 flat recommended): declaration-order reorder of `stopPolling` (AddSourceModal.tsx) and `stopSyncInterval` (useAudioSync.ts); completed `startSyncInterval` useCallback dep array (`playing`, `setPlaying`, `stopSyncInterval` — fixes a stale-closure on `playing`); typed `webkitAudioContext` cast replacing `any` (audio.ts); eslint.config.js block exempting `src/components/ui/**` (shadcn variant exports) from `react-refresh/only-export-components`; one targeted `eslint-disable-next-line react-hooks/set-state-in-effect` in useAudioSync.ts with deferral note to Phase 1.3 | `e2e/stem-split.spec.ts` (Playwright) + playwright.config.ts |
| k25 | `65b3d88` | `archive/runpod/*.py` (`handler.py`, `audio_processing.py`, `s3_ops.py`, `manifest.py`, `callbacks.py`, `config.py`, `local_test.py`) + `Dockerfile` + `requirements.txt` | `worker/` (file names kept, flat layout) | Ruff-baseline fixes only: import-block sorting (audio_processing, config, local_test), removed unused imports (`os`, `shutil` in local_test.py; `JobConfig` in handler.py), `raise RuntimeError(…) from e` in s3_ops.py. Not carried: `README.md`, `.gitignore`, `docker-compose.local.yml` | none existed upstream |
| k25 | `65b3d88` | `archive/agent/src/karaoke_agent/s3_multipart.py` | `server/src/shizzle_server/lib/s3_multipart.py` | Removed dead `TYPE_CHECKING` import of `karaoke_agent.config.S3Config`; `create_uploader_from_config(config)` now typed `Any` with docstring noting the S3Config-like duck type (original config module not salvaged) | `server/tests/lib/test_s3_multipart.py` (17 tests, imports rewritten to `shizzle_server.lib.s3_multipart`) |
| k25 | `65b3d88` | `archive/agent/src/karaoke_agent/utils/circuit_breaker.py` | `server/src/shizzle_server/lib/circuit_breaker.py` | Copied verbatim | `server/tests/lib/test_circuit_breaker.py` (12 tests, import rewritten to `shizzle_server.lib.circuit_breaker`) |
| k25 | `65b3d88` | `archive/agent/tests/conftest.py` | `server/tests/lib/conftest.py` | Trimmed to fixtures the carried tests use (`clean_env`, `s3_upload_config`, `temp_dir`, `sample_video_file`, `small_file`, `circuit_breaker`, marker config). Dropped fixtures depending on non-salvaged modules (`karaoke_agent.config`, `karaoke_agent.supabase_client`, moto S3 fixtures) | is itself test infrastructure |
| k25 | `65b3d88` | `archive/agent/pyproject.toml` | modeled into `server/pyproject.toml` and `worker/pyproject.toml` | Names/packages adapted (`shizzle-server` / `shizzle-worker`); same ruff rule set, mypy strict, pytest config. Dev tools moved from `[project.optional-dependencies]` to `[dependency-groups]` so plain `uv run` installs them; worker is `[tool.uv] package = false` (flat deploy-module layout). `uv.lock` generated for both | — |
| k25 | uncommitted working tree 2026-08-02 | `ui/src/stores/useStore.ts`, `ui/src/components/mixer/MixerDrawer.tsx`, `ui/src/hooks/useAudioSync.ts` (mute/solo feature only, isolated via `git diff` against committed baseline) | hand-ported into the copied `ui/` counterparts | Feature ported: `stemMutes`/`stemSolos` state + persistence, `toggleStemMute`/`toggleStemSolo` actions, S/M buttons in MixerDrawer, fader range −12…+6 → **−60…+12 dB** with −∞ display at −60, mute/solo-aware gain application in useAudioSync (`useAudioSync.ts` hunk included beyond the two briefed files because it is the audible half of the same feature — without it the buttons are dead UI). **Not** carried: k25's `audio.ts` `default_gain` → unity-gain change (belongs to the Phase 1.3 `default_gain_db` contract fix) | none existed upstream |

Fixture note: `fixtures/golden-30s.mp4` is not salvage — generated 2026-08-02 with ffmpeg 8.1.1 (`testsrc2` 1280x720\@30 + `aevalsrc` stereo 48 kHz bed: A2/A3/C#4 + E4 sine layers and a 2 Hz decaying 60 Hz kick), fully synthetic and rights-safe, 5.0 MB.
