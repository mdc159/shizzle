# Provenance ledger

One row per salvaged unit. Sources were **copied**, never git-merged; source
repos are read-only. "Uncommitted working tree 2026-08-02" marks files taken
from k25's dirty working tree (branch `local`) that were never committed.

Source commits at time of copy (2026-08-02):

- `k25-nextgen-rewrite` — `fd6f3f467a42a43b379f050df3551b1d9d38c153`
- `k25` — `65b3d88ffc81bd32d0444eb356715cdb751c821c` (branch `local`; archive/ paths clean at this commit)

| Source repo | Commit | Source path | Destination | Intentional changes | Tests carried |
|---|---|---|---|---|---|
| k25-nextgen-rewrite | `fd6f3f4` | `local-server/src/local_server/*.py` (`__init__.py`, `main.py`, `routes.py`, `processing.py`, `models.py`) | `library/src/shizzle_server/` | Package renamed `local_server` → `shizzle_server` (modules use relative imports, so directory rename sufficed). Ruff-baseline fixes: `lifespan(app)` → `lifespan(_app)` (main.py), `JobStatusEnum(str, Enum)` → `StrEnum` (models.py), unused `stem_wavs` param → `_stem_wavs` in `write_manifest` (processing.py), `raise HTTPException(413, …) from None` (routes.py). No behavior changes; contract renames (`default_gain` → `default_gain_db`) deferred to Phase 1.3 | none existed upstream |
| k25-nextgen-rewrite | `fd6f3f4` | `local-server/Dockerfile` | `stemsplit/Dockerfile.reference` | Copied verbatim; becomes the worker's base recipe later | — |
| k25-nextgen-rewrite | `fd6f3f4` | `ui/` (wholesale, 31 files) | `player/` | Excluded per spec "not carried": `node_modules/`, `dist/`, `README.md`, `IMPLEMENTATION_STATUS.md`, `.env.production`, `.env.example`, `vite.config.local.ts`. Lint fixes to pass ESLint (react-hooks v6 flat recommended): declaration-order reorder of `stopPolling` (AddSourceModal.tsx) and `stopSyncInterval` (useAudioSync.ts); completed `startSyncInterval` useCallback dep array (`playing`, `setPlaying`, `stopSyncInterval` — fixes a stale-closure on `playing`); typed `webkitAudioContext` cast replacing `any` (audio.ts); eslint.config.js block exempting `src/components/ui/**` (shadcn variant exports) from `react-refresh/only-export-components`; one targeted `eslint-disable-next-line react-hooks/set-state-in-effect` in useAudioSync.ts with deferral note to Phase 1.3 | `e2e/stem-split.spec.ts` (Playwright) + playwright.config.ts |
| k25 | `65b3d88` | `archive/runpod/*.py` (`handler.py`, `audio_processing.py`, `s3_ops.py`, `manifest.py`, `callbacks.py`, `config.py`, `local_test.py`) + `Dockerfile` + `requirements.txt` | `stemsplit/` (file names kept, flat layout) | Ruff-baseline fixes only: import-block sorting (audio_processing, config, local_test), removed unused imports (`os`, `shutil` in local_test.py; `JobConfig` in handler.py), `raise RuntimeError(…) from e` in s3_ops.py. Not carried: `README.md`, `.gitignore`, `docker-compose.local.yml` | none existed upstream |
| k25 | `65b3d88` | `archive/agent/src/karaoke_agent/s3_multipart.py` | `library/src/shizzle_server/lib/s3_multipart.py` | Removed dead `TYPE_CHECKING` import of `karaoke_agent.config.S3Config`; `create_uploader_from_config(config)` now typed `Any` with docstring noting the S3Config-like duck type (original config module not salvaged) | `library/tests/lib/test_s3_multipart.py` (17 tests, imports rewritten to `shizzle_server.lib.s3_multipart`) |
| k25 | `65b3d88` | `archive/agent/src/karaoke_agent/utils/circuit_breaker.py` | `library/src/shizzle_server/lib/circuit_breaker.py` | Copied verbatim | `library/tests/lib/test_circuit_breaker.py` (12 tests, import rewritten to `shizzle_server.lib.circuit_breaker`) |
| k25 | `65b3d88` | `archive/agent/tests/conftest.py` | `library/tests/lib/conftest.py` | Trimmed to fixtures the carried tests use (`clean_env`, `s3_upload_config`, `temp_dir`, `sample_video_file`, `small_file`, `circuit_breaker`, marker config). Dropped fixtures depending on non-salvaged modules (`karaoke_agent.config`, `karaoke_agent.supabase_client`, moto S3 fixtures) | is itself test infrastructure |
| k25 | `65b3d88` | `archive/agent/pyproject.toml` | modeled into `library/pyproject.toml` and `stemsplit/pyproject.toml` | Names/packages adapted (`shizzle-server` / `shizzle-worker`); same ruff rule set, mypy strict, pytest config. Dev tools moved from `[project.optional-dependencies]` to `[dependency-groups]` so plain `uv run` installs them; worker is `[tool.uv] package = false` (flat deploy-module layout). `uv.lock` generated for both | — |
| k25 | uncommitted working tree 2026-08-02 | `ui/src/stores/useStore.ts`, `ui/src/components/mixer/MixerDrawer.tsx`, `ui/src/hooks/useAudioSync.ts` (mute/solo feature only, isolated via `git diff` against committed baseline) | hand-ported into the copied `player/` counterparts | Feature ported: `stemMutes`/`stemSolos` state + persistence, `toggleStemMute`/`toggleStemSolo` actions, S/M buttons in MixerDrawer, fader range −12…+6 → **−60…+12 dB** with −∞ display at −60, mute/solo-aware gain application in useAudioSync (`useAudioSync.ts` hunk included beyond the two briefed files because it is the audible half of the same feature — without it the buttons are dead UI). **Not** carried: k25's `audio.ts` `default_gain` → unity-gain change (belongs to the Phase 1.3 `default_gain_db` contract fix) | none existed upstream |

Fixture note: `fixtures/golden-30s.mp4` is not salvage — generated 2026-08-02 with ffmpeg 8.1.1 (`testsrc2` 1280x720\@30 + `aevalsrc` stereo 48 kHz bed: A2/A3/C#4 + E4 sine layers and a 2 Hz decaying 60 Hz kick), fully synthetic and rights-safe, 5.0 MB.

## Phase 1.3/1.4 modifications to salvaged units (2026-08-02)

| Source repo | Commit | Source path | Destination | Intentional changes | Tests carried |
|---|---|---|---|---|---|
| k25-nextgen-rewrite | `fd6f3f4` | `local-server/src/local_server/processing.py` (already salvaged above) | `library/src/shizzle_server/processing.py` | Phase 1.3 contract fix: manifest stem field `default_gain` (linear, written as `0` = silence) → `default_gain_db` (dB); manifest `version` 2 → 3 | new `library/tests/test_manifest.py` (3 tests: version, default_gain_db contract, stem id/ext selection) |
| k25-nextgen-rewrite | `fd6f3f4` | `ui/src/lib/audio.ts` (`StemAudioManager`) | **deleted**; logic moved into `player/src/lib/playback/mediaElementEngine.ts` behind the new `PlaybackEngine` interface (`player/src/lib/playback/PlaybackEngine.ts`) | Element loading (15 s timeout), play/pause/seek, gain smoothing (`setTargetAtTime` 0.05) carried verbatim. Dead `syncToTime` dropped. `default_gain` applied-as-linear bug fixed: `default_gain_db` converted via `dbToLinear()` at load. New: master bus (stem gains → master GainNode with fixed −3 dB headroom per spike 0.4 → DynamicsCompressor limiter −1 dBFS/knee 0/ratio 20/attack 3 ms/release 50 ms → destination), per-stem metrics surface (`player/src/lib/playback/metrics.ts`), dB helpers extracted to `player/src/lib/playback/db.ts` (unused `formatDb` dropped) | exercised by `evidence/spikes/skew-probe` pattern rerun (Phase 1.4 headless playback check) |
| k25-nextgen-rewrite + k25 uncommitted | `fd6f3f4` + working tree 2026-08-02 | `ui/src/hooks/useAudioSync.ts` (drift/stall policy + mute/solo gain application) | drift/stall policy → `player/src/lib/playback/driftPolicy.ts` (pure) + `mediaElementEngine.ts` sync loop; mute/solo gain math → `mediaElementEngine.applyStemGains()`; hook rewritten as thin adapter over the engine | Policy constants and tiers ported unchanged (soft 50 ms / nudge ≤±0.5% / hard 250 ms / 3 stall ticks / 1 s cadence — spike 0.1 validated, 3 ms desktop skew). Stall bailout now surfaces via `onStallBailout` callback (same toast + pause behavior). New: master volume wired store → `setMasterGainDb` (slider linear 0..1 → dB); previously the volume slider was dead | — |
| k25-nextgen-rewrite | `fd6f3f4` | `ui/src/stores/useStore.ts`, `ui/src/components/player/TransportControls.tsx`, `ui/src/components/mixer/MixerDrawer.tsx`, `ui/src/components/player/PlayerShell.tsx`, `ui/src/types/karaoke.ts`, `ui/src/main.tsx` | same paths | Dead `buffering` flag removed (store + TransportControls spinner branch). TransportControls: limiter-activity amber dot (250 ms metrics poll while playing). MixerDrawer: direct `audioManager` calls removed — store is the single write path, engine follows via the hook. PlayerShell: manifest-load effect deps destructured (lint). `Stem.default_gain` → `default_gain_db`. main.tsx: dev-only `window.__shizzle` test hook (engine + store) | — |
| k25-nextgen-rewrite | `fd6f3f4` | `local-server/Dockerfile` (via `stemsplit/Dockerfile.reference`) | `library/Dockerfile` | Adapted for `shizzle_server`: uvicorn target `shizzle_server.main:app`, copies `README.md` (hatchling), dropped nextgen-only `DEMUCS_DIRECT_MODE` env. CUDA 12.1.1 base, torch cu121, demucs layers unchanged | container healthcheck (torch.cuda + demucs import) |
| k25-nextgen-rewrite | `fd6f3f4` | `docker-compose.yml` | `deploy/vps/compose.yml` (`local` profile) | Service renamed `shizzle-local-server`; same GPU reservation, port 8001, data bind mount (now `../../data` relative to `deploy/vps/`); added `profiles: ["local"]` | — |

## Phase 2 modifications to salvaged units (2026-08-02)

New Phase 2 units (`library/src/shizzle_server/db/`, `orchestrator/`, `settings.py`, `errors.py`, `library/alembic/`, `library/Dockerfile.api`, `deploy/vps/caddy/Caddyfile.local`, `library/tests/{conftest,test_repository,test_state_machine,test_orchestrator_unit,test_api}.py`, `library/tests/contract/`) are original work, not salvage — designed against design spec §4 and the read-only worker staging contract (`stemsplit/MANIFEST.md`).

| Source repo | Commit | Source path | Destination | Intentional changes | Tests carried |
|---|---|---|---|---|---|
| k25-nextgen-rewrite | `fd6f3f4` | `local-server/src/local_server/routes.py` (already salvaged) | `library/src/shizzle_server/routes.py` | Phase 2 rewrite on the database: in-memory `jobs` dict and filesystem-scan library **replaced** by `jobs`/`tracks` tables via the typed repository layer. Carried verbatim: chunked streaming upload with 2 GB cap, ffprobe duration gate, path-traversal guard on file serving. New: `POST /api/submit-url` (fails at the Phase 3 yt-dlp stub with structured `YTDLP_BLOCKED` — intended), `GET /api/jobs` (paginated history), `DELETE /api/tracks/{id}` (soft delete), `GET /api/health` (db + orchestrator heartbeat liveness). Dropped: `MAX_JOBS`/`_enforce_retention` silent retention deletion — design §4 says "No silent retention deletion; manual delete in UI" (design authority, Mike 2026-08-02). Upload now also records sha256 `input_checksum` while streaming | `library/tests/test_api.py` (7 tests) |
| k25-nextgen-rewrite | `fd6f3f4` | `local-server/src/local_server/models.py` (already salvaged) | `library/src/shizzle_server/models.py` | Rewritten as API response schemas over the new DB models; `JobStatusEnum` aliased to the §4 `JobStage` enum (`pending → downloading → dispatched → splitting → verifying → publishing → ready | failed`), replacing the Phase 1 `extracting/encoding` statuses. `TrackInfo.publicUrl` now `/api/tracks/{track_uuid}` | covered by test_api.py |
| k25-nextgen-rewrite | `fd6f3f4` | `local-server/src/local_server/main.py` (already salvaged) | `library/src/shizzle_server/main.py` | `create_app()` factory; DB engine + repositories on `app.state`; SQLite `create_all` on startup (Postgres schema is Alembic-owned); optional embedded orchestrator task (`SHIZZLE_EMBEDDED_ORCHESTRATOR`, default true) so the single-container `local` GPU profile keeps working with no Postgres — **agent working default**, held lightly: the alternative (Postgres added to the local profile) was rejected to keep the local profile's one-container contract unchanged | test_api.py exercises lifespan |
| k25-nextgen-rewrite | `fd6f3f4` | `docker-compose.yml` (via `deploy/vps/compose.yml`) | `deploy/vps/compose.yml` | `local` profile untouched. Added `stack` profile (api on `Dockerfile.api` slim image running `alembic upgrade head` before uvicorn, orchestrator `python -m shizzle_server.orchestrator`, postgres:16-alpine with named volume `pgdata` + healthcheck + host port 127.0.0.1:5433, caddy:2 on `${CADDY_HTTP_PORT:-80}` with `deploy/vps/caddy/Caddyfile.local`) and `test` profile (postgres only, for the contract suite). Machine gotcha recorded: global `POSTGRES_*` env vars from another project leak into compose interpolation — pin them in `.env` on any machine running the stack | `library/tests/contract/` (9 fault-injection tests on the real compose Postgres) |
| — | — | — | `player/src/types/karaoke.ts`, `player/src/components/source/AddSourceModal.tsx` | Minimal API-contract sync: `JobStatus.status` union moved to the §4 stages (+ optional `errorCode`, `attempt`, `stageTimings`, `trackId`); modal STEPS list updated to the new stage labels. `eslint`/`tsc` green | — |
